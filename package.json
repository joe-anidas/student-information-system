{
  "name": "student-information-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "serve": "cd backend && node server.js" ,"dev": "cd frontend && npm run dev"
  },
  "dependencies": {
    "concurrently": "^8.2.1"
  }
}
